<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/png" href="{{ image('img/lila.png', 40, 0, 70, 'ico') }}" />
    <title>{{ title }}</title>
    <link rel="stylesheet" href="{{ asset('css/lila.css') }}" />
  </head>
  <body>
    <main class="container text-center mt-4 mb-6">
      <h1 class="mb-2">{{ translate("welcome") }}</h1>

      <div class="flex justify-center mb-4">
        <img src="{{ image('img/lila.png', 200) }}" width="200" alt="LilaPHP" />
      </div>

      <p class="mt-2 mb-4 text-lg">
        {{ translate("intro_text") }}
      </p>

      <section class="mt-6 text-left max-w-2xl mx-auto">
        <h2>🚀 {{ translate("features_title") }}</h2>
        <ul class="list-disc ml-6 mt-2 space-y-1">
          <li>{{ translate("feature_minify") }}</li>
          <li>{{ translate("feature_seo") }}</li>
          <li>{{ translate("feature_middlewares") }}</li>
          <li>{{ translate("feature_validation") }}</li>
          <li>{{ translate("feature_security") }}</li>
          <li>{{ translate("feature_routing") }}</li>
          <li>{{ translate("feature_compatibility") }}</li>
        </ul>
      </section>

      <section class="mt-8">
        <h2>⭐ {{ translate("support_title") }}</h2>
        <p class="mt-2">
          {{ translate("support_text") }}
          <br />
          <a
            href="https://github.com/seip25/LilaPHP"
            target="_blank"
            class="underline text-blue-500 hover:text-blue-700"
          >
            GitHub: seip25/LilaPHP
          </a>
        </p>
      </section>

      <div class="mt-8 flex justify-center gap-6">
        <a href="/set-lang/?lang=eng">🇺🇸 English</a>
        <a href="/set-lang/?lang=esp">🇪🇸 Español</a>
        <a href="/set-lang/?lang=br">🇧🇷 Português</a>
      </div>
    </main>
  </body>
</html>
