# Deny access to all files in app/ directory
# This protects the framework core, CLI, models, and configs from web access

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
