name: ${APP_NAME:-lilaphp}

services:
  nginx:
    image: nginx:alpine
    container_name: ${APP_NAME:-lilaphp}-nginx
    restart: unless-stopped
    ports:
      - "${HTTP_PORT:-8080}:80"
    env_file:
      - ./backend/.env
    volumes:
      - ./docker/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - .:/var/www/html:ro
    depends_on:
      - php
    networks:
      - lila-network

  php:
    build:
      context: .
      dockerfile: docker/php/Dockerfile.${APP_ENV:-dev}
    container_name: ${APP_NAME:-lilaphp}-fpm
    restart: unless-stopped
    volumes:
      - .:/var/www/html
    env_file:
      - ./backend/.env
    environment:
      - APP_ENV=${APP_ENV:-development}
      - APP_DEBUG=${APP_DEBUG:-true}
      - DB_HOST=mysql
      - REDIS_HOST=redis
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_started
    networks:
      - lila-network

  node:
    image: node:24-alpine
    container_name: ${APP_NAME:-lilaphp}-node
    restart: unless-stopped
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "5173:5173"
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
    networks:
      - lila-network

  mysql:
    image: mysql:8.0
    container_name: ${APP_NAME:-lilaphp}-mysql
    restart: unless-stopped
    ports:
      - "${DB_PORT:-3306}:3306"
    env_file:
      - ./backend/.env
    environment:
      MYSQL_DATABASE: ${DB_NAME:-lilaphp}
      MYSQL_USER: ${DB_USER:-lila}
      MYSQL_PASSWORD: ${DB_PASSWORD:-secret}
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-secret}
    volumes:
      - mysql-data:/var/lib/mysql
    command: --default-authentication-plugin=mysql_native_password --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    healthcheck:
      test: [ "CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${DB_PASSWORD:-secret}" ]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - lila-network

  redis:
    image: redis:7-alpine
    container_name: ${APP_NAME:-lilaphp}-redis
    restart: unless-stopped
    ports:
      - "${REDIS_PORT:-6379}:6379"
    env_file:
      - ./backend/.env
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes
    networks:
      - lila-network

networks:
  lila-network:
    name: ${APP_NAME:-lilaphp}-lila-network
    driver: bridge

volumes:
  mysql-data:
    name: ${APP_NAME:-lilaphp}-mysql-data
  redis-data:
    name: ${APP_NAME:-lilaphp}-redis-data
