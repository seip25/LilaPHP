# Deny all direct web/HTTP access to backend models, routes, jobs, and configuration files
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
