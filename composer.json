{
  "name": "seip25/lila-php",
  "description": "Ultra-fast API-First PHP 8.4+ framework with Nginx File-Based Routing, OPcache Preload in RAM, Dual-Tier Cache (APCu + Redis), Background Workers, and Blue-bird style CLI.",
  "type": "project",
  "keywords": ["php", "api", "framework", "opcache", "apcu", "redis", "fastcgi", "high-performance"],
  "license": "MIT",
  "autoload": {
    "psr-4": {
      "Core\\": "_core/",
      "Models\\": "backend/models/",
      "Cli\\": "_core/cli/"
    }
  },
  "require": {
    "php": ">=8.4"
  },
  "suggest": {
    "ext-pdo_mysql": "Required for MySQL database connection pooling.",
    "ext-apcu": "Recommended for zero-latency shared RAM caching.",
    "ext-redis": "Recommended for distributed caching and background job queues.",
    "ext-opcache": "Required for permanent in-memory preloading in production.",
    "ext-curl": "Required for Core\\Http client requests."
  }
}
